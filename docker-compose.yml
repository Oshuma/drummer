services:
  drummer:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./uploads:/app/uploads
      - ./processed:/app/processed
      - ./data:/app/data
      - ./temp:/app/temp
    restart: unless-stopped
